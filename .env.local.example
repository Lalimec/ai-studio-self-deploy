# AI Studio Environment Variables
# Copy this file to .env.local and fill in your values

# ============================================================
# GEMINI API KEY (Required)
# ============================================================
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ============================================================
# FIREBASE CONFIGURATION (Required for Authentication)
# ============================================================
# Get these values from Firebase Console > Project Settings > General > Your apps > Web app
# See .docs/FIREBASE_SETUP.md for detailed instructions

VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789012
VITE_FIREBASE_APP_ID=1:123456789012:web:abcdef123456

# ============================================================
# ACCESS CONTROL (Optional)
# ============================================================
# Comma-separated list of allowed email domains
# Default: lyrebirdstudio.net
VITE_ALLOWED_DOMAINS=lyrebirdstudio.net,yourdomain.com

# Allow users from domains not in ALLOWED_DOMAINS
# If false, only users from ALLOWED_DOMAINS can sign up
# If true, external users can sign up but need approval
# Default: false
VITE_ALLOW_EXTERNAL=false

# ============================================================
# SERVER-SIDE FIREBASE ADMIN SDK (Production Only)
# ============================================================
# For local development, use GOOGLE_APPLICATION_CREDENTIALS instead
# For Cloud Run production, set FIREBASE_SERVICE_ACCOUNT_JSON in GCP

# GOOGLE_APPLICATION_CREDENTIALS=./service-account.json
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}

# ============================================================
# NOTES
# ============================================================
# 1. NEVER commit .env.local to git (it's in .gitignore)
# 2. For production deployment, set these in Cloud Run environment variables
# 3. Frontend variables must start with VITE_ to be exposed by Vite
# 4. See .docs/FIREBASE_SETUP.md for complete setup instructions
